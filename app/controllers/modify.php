<?php
class modify extends BaseController {

    public function form()
    {
        // $form = Form::model($user, array('route' => array('user.update', $user->id)));
        // $candidate=new candidate();
        // return View::make('form',array(
        //     'const' => $this->collect_const(),
        //     'candidate' => $candidate,
        //     'type' => $type
        // ));
        return 'preparing...';
    }

    public function form_sent()
    {
        // $form = Form::model($user, array('route' => array('user.update', $user->id)));
        // $candidate=new candidate();
        // return View::make('form',array(
        //     'const' => $this->collect_const(),
        //     'candidate' => $candidate,
        //     'type' => $type
        // ));
        return 'preparing...';
    }

}
